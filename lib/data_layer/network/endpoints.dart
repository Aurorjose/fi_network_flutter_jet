/// Holds the endpoints to access the API.
class Endpoints {
  /// Creates a new [Endpoints] object.
  const Endpoints();

  /// The base url.
  static const String baseUrl = 'https://gateway.finetwork.com';

  /// The products endpoint.
  static const String products = '/product/offering/products';
}
